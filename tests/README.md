# tests/

Cross-cutting integration tests spanning frontend and backend.

Phase 1 backend tests live in `backend/tests/`. Frontend tests live in `frontend/src/**/*.test.ts`.

Run all tests: `make test`
